# WordPress Configuration
WORDPRESS_URL=https://your-wordpress-site.com
WORDPRESS_USERNAME=your_admin_username
WORDPRESS_PASSWORD=your_admin_password
# Set to true if using WordPress Application Passwords (recommended for WP 5.6+)
WORDPRESS_USE_APP_PASSWORD=false

# Database Configuration
# Examples:
# SQLite: sqlite:///leads.db
# MySQL: mysql+pymysql://user:password@localhost/dbname
# PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///leads.db

# Extraction Settings
# How often to run extraction (in minutes)
EXTRACTION_INTERVAL_MINUTES=60
# How many days back to look for leads on first run
LOOK_BACK_DAYS=7

# Go High Level Configuration
# Get access token from GHL OAuth or API settings
GHL_ACCESS_TOKEN=your_ghl_access_token
# Location ID (sub-account ID) from GHL
GHL_LOCATION_ID=your_location_id
# API version (optional, defaults to 2021-07-28)
GHL_API_VERSION=2021-07-28
# Sync settings
GHL_BATCH_SIZE=10
GHL_MAX_RETRIES=3
GHL_RETRY_DELAY_MINUTES=30

# Optional: Email notifications (future feature)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# NOTIFICATION_EMAIL=
